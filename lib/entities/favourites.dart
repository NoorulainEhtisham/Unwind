import 'package:flutter/material.dart';

class Favourites{
  String title;
  String path;
  String id;
  Icon icon;

  Favourites({
    required this.title,
    required this.path,
    required this.id,
    required this.icon,
  });
}